export const Constants = Object.freeze({
    UserSettings: Object.freeze({
        DARK_THEME: 'dark',
        LIGHT_THEME: 'light',
        THEME_NAME: 'theme'
    }),
    Endpoints: Object.freeze({

    })
});